import React from "react";
import "./Task.css";

const Task = props => 
    <div className="task-wrapper">;
        <div> Tasks Will go here, will have to wait for Create task components. Creating Create task components next that will take the information from the database</div>
    </div>

export default Task;
